import 'package:flutter/material.dart';

class AppColors {
  // 🔴 British Hub Red (Primary)
  static const Color primaryRed = Color(0xFFD91E2C);

  // 🔵 British Hub Blue (Secondary)
  static const Color secondaryBlue = Color(0xFF1F3C88);

  // ⚪ Base Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color backgroundgrey = Color(0xFFF6F7F9);

  // 🖤 Text
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF7A7A7A);

  // 🩶 Border & Divider
  static const Color border = Color(0xFFE5E5E5);

  // ❤️ Soft Highlight
  static const Color softRed = Color(0xFFFFF1F1);
}
